#!/bin/bash
 
# 设置进程数量
n=120
 
# 使用循环启动进程
for ((i=1; i<=n; i++)); do
  # 这里可以替换为你想要启动的进程命令
  # 例如：你可以使用 "echo Hello" 来打印 "Hello" 消息
  # 或者使用 "sleep 10" 来启动一个休眠10秒的进程
  # 或者其他任何你想要启动的进程
  $1 $i 1111 &
  # 实际启动进程的命令放在这里
done